# House rules

Generated by `uxproof init` from what this repo actually does. Regenerate with `uxproof sync`; only the manual section below survives regeneration — put your team's judgment calls there.

## Stack

- Framework: react (workspace/monorepo)
- Styling: tailwind (tailwind v4)
- validation: zod
- icons: lucide-react

## Design tokens

67 custom properties found (62 colors). Sources:

- `src\styles\global.css` (98 tokens)

Rule: colors come from tokens. A raw hex/rgb value in component code is a finding unless the manual section grants an exception.

## Components

15 components registered from:

- `src/components` (15 files)

Rule: prefer the house component over the raw element:

- `<button>` → `<Button>`

## Manual rules

<!-- uxproof:manual-start -->

_Written 2026-08-21 during the first `om-ux-setup` run. These rules outrank the generated
sections above on conflict._

### Scope of this contract

This contract governs **the web application only** — Astro pages in `src/pages/`, the layout in
`src/layouts/`, and components in `src/components/`.

`packages/code-reviewer/` is out of scope. It is a Node CLI for AI-assisted code review, not a UI
surface. The generated Stack section above lists `validation: zod` and marks the repo as a
workspace/monorepo **because of that package** — the web app depends on neither. Ignore both
signals when judging UI.

### Known inaccuracies in the generated sections

The extractor does not understand Astro, so it reports the framework as `react` and registers only
`.tsx` files. Read the generated sections with these corrections in mind:

- **The framework is Astro 6**; React 19 appears as islands (`client:load`), not as the app shell.
- **The component registry covers 15 of ~30 UI files (~50%).** Invisible to it: 5 `.astro`
  components (`Topbar`, `Banner`, `RetentionBanner`, `Welcome`, `ui/LibBadge`), `layouts/Layout.astro`,
  and all 9 pages under `src/pages/`. A finding's absence in those files is not evidence of health.
- **`archetypes` is empty for the same reason** — every screen lives in a `.astro` page. There is no
  extracted canonical example to compare a new screen against; judge screens against the app itself.
- **13 of 31 color tokens describe features this product does not have**: `sidebar*` (8) and
  `chart-1..5` (5). They are unused shadcn/ui defaults inherited from the starter. Do not treat them
  as house palette, and do not cite their absence from the code as a finding.

### Light/dark drift — known, unresolved

`src/styles/global.css` declares every color twice: `:root` (light) and `.dark` (dark). Three facts
that do not line up, recorded here so nobody re-derives them:

1. **`tokens.json` carries the `.dark` values.** The extractor de-duplicates by name and keeps the
   last declaration, so `background = oklch(0.145 0 0)` is the dark value.
2. **The running app resolves `:root`, i.e. the light palette.** The `.dark` class is never applied
   anywhere in `src/`.
3. **No token drives the page background at all.** `body` uses the hardcoded `bg-cosmic` utility
   (`linear-gradient(#0a0e1a, #0f1529, #0a0e1a)`).

**Do not trust the values in `tokens.json` as a description of what renders.** The token _names_ are
sound; the values are from the branch the app does not use.

Which theme the product should target is deliberately **not decided here**. Treat it as an open
question until the UX shaping pass resolves it; do not file findings that presuppose either answer.

### Legacy starter styling — do not extend

The dominant visual pattern in the code is inherited from `przeprogramowani/10x-astro-starter`:

- the `bg-cosmic` background utility,
- the glass-card recipe `rounded-2xl border border-white/10 bg-white/10 backdrop-blur-xl`,
- gradient headings `bg-gradient-to-r from-blue-200 to-purple-200 bg-clip-text text-transparent`,
- hardcoded `blue-*` / `purple-*` / `white/N` utilities throughout.

**Frequency is not intent.** This pattern is widespread (247 hardcoded color utilities against 4
token-based ones as of 2026-08-21) because it was copied forward from the starter, not because the
team chose it. Accordingly:

- It is **not** a house pattern. Never cite it as the example a new screen should follow.
- New and rewritten UI uses the semantic tokens above; migrating toward them is the intended
  direction.
- Existing occurrences are **legacy, not a backlog of findings**. Do not flood a review with one
  finding per occurrence; raise the pattern once, where it matters for the change under review.

### Naming and formatting (carried from `AGENTS.md`)

- React components: `PascalCase.tsx`, grouped in feature folders (`auth/`, `generate/`, `library/`,
  `review/`, `settings/`); shared primitives in `ui/`.
- Route files: lowercase paths. Utilities: lowercase or kebab-case.
- Application logic in TypeScript; pages and layouts stay `.astro`.
- Intentionally unused identifiers are prefixed with `_`.
- Prettier: `printWidth: 120`, double quotes.

<!-- uxproof:manual-end -->
