W repo projektu:

```bash
npx @przeprogramowani/10x-cli@latest get m2l5


UI Prompt for Claude:
During work on S-01 through S-04. I noticed missing: bulk actions on candidate review, no way to reset a review session, poor loading states. After loging user stays on home page where after loging there is still sign in/sign up screen, it should go directly to dashboard or logged in home page should become dashboard. Dashboard page has box with 3 actions instead it will be better to present those action on top as banner it will allow user to quickly navigate between pages without returning to dashboard. Card Library has no pagination and has no simple keyword search. Add a new slice S-06 "UX improvements" to the roadmap — status planned, prerequisites F-01, parallel with S-05. Keep it concise and consistent with existing slice format.