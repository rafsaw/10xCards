# Archived (superseded) docs — srs-review-session

Local archive for documents no longer on the active path. **Not** the repo-wide
`context/archive/` (which is immutable and reserved for whole archived changes via `/10x-archive`)
— this is just a within-change tidy.

## supermemo-docs.md

SM-2 / `supermemo` (Context7) API reference. **Superseded for v1** by the decision in
`../algorithm-decision.md` to use **Leitner** (see `../leitner-docs.md`). SM-2 needed a new
`efactor` column and its per-card adaptivity is wasted under binary right/wrong rating (FR-014).

Kept, not deleted, because SM-2 / FSRS is the explicit **v2 path** if rating ever becomes
multi-grade — at which point this reference is the starting point again. The library survey that
weighed all candidates lives in `../external-research.md`.
